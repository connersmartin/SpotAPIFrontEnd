@{
    ViewData["Title"] = "Make a playlist";
}

<div class="text-center">
    <h1 class="display-4">Do you want to build a playlist?</h1>
    <button id="authBtn" onclick="location.href='@Url.Action("Auth","Home")'">Do it</button>
</div>
<partial id="view">
    @Html.Partial("SpotParams", new Spot { })
</partial>
 
